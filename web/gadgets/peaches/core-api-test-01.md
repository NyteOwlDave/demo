
// TreeView Peach
// Testing core-api & treeview

// ID = core-api-test.js

/*
stg=sessionStorage;
rsk=i=>stg.keys(i);
rse=k=>stg.getItem(k);
wse=(k,v)=>stg.setItem(k,v);
look=()=>show(tmp(),"Session");

k="look.js";
v="look()";
wse(k,v);
srun(k);
*/

API = { wrun, zrun, run, krun, srun };
// show( API );
inspect( API, "Run Methods" );
